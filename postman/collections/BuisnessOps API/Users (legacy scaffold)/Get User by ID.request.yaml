$kind: http-request
description: Same caveat as List Users — unauthenticated, untenanted legacy scaffold.
url: "{{baseUrl}}/users/{{ownerUserId}}"
method: GET
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status is 200', function () {
        pm.response.to.have.status(200);
      });
    language: text/javascript
order: 2000
